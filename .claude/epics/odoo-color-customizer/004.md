---
name: CSS Controller
status: open
created: 2026-01-06T16:44:00Z
updated: 2026-01-06T16:44:00Z
github: [Will be updated when synced to GitHub]
depends_on: [001, 002]
parallel: true
conflicts_with: []
---

# Task: CSS Controller

## Description
Create a controller endpoint that returns dynamic CSS with the custom primary color. The CSS overrides Odoo's default CSS variables. Includes color calculation utilities for hover/active states.

## Acceptance Criteria
- [ ] Controller endpoint: `/color_customizer/theme.css`
- [ ] Returns CSS with Content-Type `text/css`
- [ ] Reads color from `ir.config_parameter`
- [ ] Calculates derived colors (hover, active, light, text)
- [ ] Falls back to default if no color set
- [ ] Cacheable response headers

## Technical Details

### File: `controllers/main.py`
```python
from odoo import http
from odoo.http import request

DEFAULT_PRIMARY_COLOR = '#714B67'

class ColorCustomizerController(http.Controller):

    @http.route('/color_customizer/theme.css', type='http', auth='public', cors='*')
    def get_theme_css(self):
        """Return dynamic CSS with custom primary color"""
        primary_color = request.env['ir.config_parameter'].sudo().get_param(
            'odoo_color_customizer.primary_color',
            DEFAULT_PRIMARY_COLOR
        )

        # Calculate color variants
        hover_color = self._darken_color(primary_color, 0.1)
        active_color = self._darken_color(primary_color, 0.2)
        light_color = self._lighten_color(primary_color, 0.85)
        text_color = self._get_contrast_color(primary_color)

        css = f"""
:root {{
    --custom-primary: {primary_color};
    --custom-primary-hover: {hover_color};
    --custom-primary-active: {active_color};
    --custom-primary-light: {light_color};
    --custom-primary-text: {text_color};

    /* Override Odoo brand colors */
    --o-brand-odoo: {primary_color};
    --o-brand-primary: {primary_color};
}}
"""
        return request.make_response(
            css,
            headers=[
                ('Content-Type', 'text/css'),
                ('Cache-Control', 'public, max-age=3600'),
            ]
        )

    def _hex_to_rgb(self, hex_color):
        """Convert hex color to RGB tuple"""
        hex_color = hex_color.lstrip('#')
        return tuple(int(hex_color[i:i+2], 16) for i in (0, 2, 4))

    def _rgb_to_hex(self, rgb):
        """Convert RGB tuple to hex color"""
        return '#{:02x}{:02x}{:02x}'.format(*[max(0, min(255, int(c))) for c in rgb])

    def _darken_color(self, hex_color, amount):
        """Darken a hex color by a percentage"""
        r, g, b = self._hex_to_rgb(hex_color)
        factor = 1 - amount
        return self._rgb_to_hex((r * factor, g * factor, b * factor))

    def _lighten_color(self, hex_color, amount):
        """Lighten a hex color by a percentage"""
        r, g, b = self._hex_to_rgb(hex_color)
        return self._rgb_to_hex((
            r + (255 - r) * amount,
            g + (255 - g) * amount,
            b + (255 - b) * amount
        ))

    def _get_contrast_color(self, hex_color):
        """Return white or black based on background luminance"""
        r, g, b = self._hex_to_rgb(hex_color)
        luminance = (0.299 * r + 0.587 * g + 0.114 * b) / 255
        return '#ffffff' if luminance < 0.5 else '#000000'
```

### File: `controllers/__init__.py`
```python
from . import main
```

### Endpoint Details
- **URL**: `/color_customizer/theme.css`
- **Auth**: Public (needed for CSS load before login)
- **Response**: CSS text with custom properties
- **Cache**: 1 hour

## Dependencies
- [ ] Task 001 (Module Scaffold) complete
- [ ] Task 002 (Settings Model) for ir.config_parameter

## Effort Estimate
- Size: M
- Hours: 2
- Parallel: true (can run with frontend tasks)

## Definition of Done
- [ ] Endpoint returns valid CSS
- [ ] Color calculations correct
- [ ] Default fallback works
- [ ] No authentication errors
- [ ] Git committed
