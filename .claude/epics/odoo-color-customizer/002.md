---
name: Settings Model
status: open
created: 2026-01-06T16:44:00Z
updated: 2026-01-06T16:44:00Z
github: [Will be updated when synced to GitHub]
depends_on: [001]
parallel: true
conflicts_with: []
---

# Task: Settings Model

## Description
Extend `res.config.settings` to add the primary color configuration field. The color value is stored in `ir.config_parameter` for persistence across sessions.

## Acceptance Criteria
- [ ] `res.config.settings` extended with `primary_color` field
- [ ] Color stored in `ir.config_parameter` key: `odoo_color_customizer.primary_color`
- [ ] Default value: `#714B67` (Odoo purple)
- [ ] Field uses Odoo's built-in color widget
- [ ] Reset to default method implemented
- [ ] Color value persists after save

## Technical Details

### File: `models/res_config_settings.py`
```python
from odoo import api, fields, models

DEFAULT_PRIMARY_COLOR = '#714B67'

class ResConfigSettings(models.TransientModel):
    _inherit = 'res.config.settings'

    primary_color = fields.Char(
        string='Primary Color',
        config_parameter='odoo_color_customizer.primary_color',
        default=DEFAULT_PRIMARY_COLOR,
        help='Choose a custom primary color for the Odoo interface'
    )

    def action_reset_primary_color(self):
        """Reset primary color to Odoo default purple"""
        self.env['ir.config_parameter'].sudo().set_param(
            'odoo_color_customizer.primary_color',
            DEFAULT_PRIMARY_COLOR
        )
        return {
            'type': 'ir.actions.client',
            'tag': 'reload',
        }
```

### File: `models/__init__.py`
```python
from . import res_config_settings
```

### System Parameter
- Key: `odoo_color_customizer.primary_color`
- Default: `#714B67`

## Dependencies
- [ ] Task 001 (Module Scaffold) must be complete

## Effort Estimate
- Size: S
- Hours: 1
- Parallel: true (can run with Task 003)

## Definition of Done
- [ ] Model code implemented
- [ ] Field saves to ir.config_parameter
- [ ] Default value works correctly
- [ ] Reset action functional
- [ ] Git committed
